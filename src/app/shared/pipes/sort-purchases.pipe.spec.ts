import { SortPurchasesPipe } from './sort-purchases.pipe';

describe('SortPurchasesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPurchasesPipe();
    expect(pipe).toBeTruthy();
  });
});
