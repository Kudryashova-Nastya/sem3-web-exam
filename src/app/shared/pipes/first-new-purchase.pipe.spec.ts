import { FirstNewPurchasePipe } from './first-new-purchase.pipe';

describe('FirstNewPurchasePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstNewPurchasePipe();
    expect(pipe).toBeTruthy();
  });
});
